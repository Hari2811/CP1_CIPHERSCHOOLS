#include<iostream>
using namespace std;
int main(){
	int num = 0,rem=0,sum=0,temp=0;
	cout<<"Enter Number";
	cin>>num;
	temp = num;
	while (num!=0){
		rem = num%10;
		sum = (sum)+(rem*rem*rem);
		num = num/10;
	}
	if (sum==temp){
		cout<<"Yes";
	}
	else{
		cout<<"No";
	}
	return 0;
}
